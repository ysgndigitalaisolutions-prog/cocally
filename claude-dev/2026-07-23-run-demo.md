# 2026-07-23 — Run the demo

Asked to bring the demo up. Found the API (:4000) and web (:3000) dev servers already running from a previous session (`./start.sh` hit EADDRINUSE on both ports), with Mongo healthy in docker compose and seed data already applied.

Verified instead of restarting:

- `GET /api/v1/ops/health` → `{"status":"ok","db":"connected"}`
- `POST /api/v1/auth/login` with `admin` / `1234` → JWT issued
- `http://localhost:3000` → redirects to `/dashboard`, 200

No code changes. Logins documented in [LOGINS.md](LOGINS.md).
