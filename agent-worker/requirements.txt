# CoCally LiveKit Agents worker — pinned to the versions the pilot was tested on.
# Deepgram = STT (+ default Aura TTS); Groq (OpenAI-compatible plugin) = LLM.
livekit-agents[deepgram,openai,silero,turn-detector]==1.6.6
# Optional lower-TTFB voices (selected with TTS_PROVIDER=elevenlabs|cartesia):
livekit-plugins-elevenlabs==1.6.6
livekit-plugins-cartesia==1.6.6
# Optional LiveKit Cloud noise cancellation tuned for PSTN audio (NOISE_CANCELLATION=1):
livekit-plugins-noise-cancellation~=0.2
python-dotenv~=1.0
aiohttp~=3.9
